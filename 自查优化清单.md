# 自查优化清单

这份清单记录我基于当前项目状态做的自查结果，后续默认按这个顺序继续优化。

## 本轮已完成

- [x] 景区详情页重构为“游中中枢页”，补齐到导航、美食、设施、室内导航和日记的入口
- [x] 查询中心补齐“查到结果后的下一步动作”，减少用户不知道下一步去哪的问题
- [x] 美食推荐页补齐到导航、社交、日记和景区详情的链路
- [x] 路径规划页支持承接景区上下文，并优先围绕当前景区选点
- [x] 摄影打卡组件改为统一中文版本，补齐热门照片、点位详情、地图和上传流程
- [x] 地图组件统一图例、起终点标记、底图说明和路径来源文案
- [x] 修复 `featureService` 中重复拼接 `/api` 的潜在接口路径问题
- [x] 美食探索组件和本地示例数据改成统一中文版本，补齐空状态、营业提醒、打卡清单和组合推荐文案
- [x] 一日计划页修复历史脏数据导致的 `.map is not a function`，并重构为“兴趣 -> 推荐 -> 计划 -> 执行”流程
- [x] 室内导航页补上首次使用引导、失败恢复、空状态和更完整的结果展示
- [x] “一日计划 -> 户外导航 -> 美食 / 日记 / 社交”的自动衔接已补上上下文传递、返回智能行程和动态下一步入口
- [x] store 层核心切片统一为中文错误提示，并抽离公共错误解析工具
- [x] 执行清单与自查清单文档重写为正常中文，便于后续继续推进
- [x] `api / userService / pathPlanningService / reminderService / featureService / photoSpotService` 已完成一轮基础清理
- [x] 重写共享类型文件 `frontend/src/types/index.ts`，把核心实体统一为当前前端实际使用的字符串 ID 结构
- [x] 重构 `indoorNavigationService` 的本地示例数据与兜底算法，保留接口不变并增强弱网回退可读性
- [x] 重写 `featureSlice`，统一摄影打卡、美食推荐、美食路线、个性提醒的中文错误提示
- [x] 室内导航、成就、社交、日记、智能行程、查询页等高频模块已切到 `unknown + resolveErrorMessage` 的统一错误处理
- [x] 摄影打卡、景区详情、查询页进一步补齐类型约束，显著减少组件层 `any`
- [x] 全局应用壳层重构为“固定侧边栏 + 工作区顶部栏”的桌面结构，首页与景区浏览页的信息层级同步优化
- [x] 数据库层补齐景区、景点、设施的高频查询索引，数据库优化从“结构与索引”入手而不是盲目换库

## 仍然不够顺手的地方

- [x] 户外路径规划已补齐一日计划上下文记忆、上一段 / 下一段切换和返回行程入口
- [ ] 个别 service / slice 里的注释、日志和类型风格还不够统一
- [ ] 部分组件仍存在少量 `any` 类型和老式数据处理方式，长期维护成本偏高
- [ ] 弱网、接口失败、空状态的提示还可以再分层，做得更像成熟产品

## 下一步默认顺序

1. 继续整理剩余 service 层的类型和注释，重点是室内导航与日记相关服务
2. 继续清理少量 `any` 类型和旧数据处理逻辑
3. 补更明确的弱网、失败恢复和空状态细节
4. 继续收口剩余页面的按钮层级和状态提示
